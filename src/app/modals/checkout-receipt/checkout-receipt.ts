import { InjectionToken } from "@angular/core";
import { ComponentType } from "ngx-toastr";

export const CHECKOUT_RECEIPT_TOKEN: InjectionToken<any> = new InjectionToken<any>('CHECKOUT_RECEIPT_TOKEN');
