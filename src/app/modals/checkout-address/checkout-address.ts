import { InjectionToken } from "@angular/core";
import { ComponentType } from "ngx-toastr";

export const CHECKOUT_ADDRESS_TOKEN: InjectionToken<any> = new InjectionToken<any>('CHECKOUT_ADDRESS_TOKEN');
