import { InjectionToken } from "@angular/core";
import { ComponentType } from "ngx-toastr";

export const CHECKOUT_IZIPAY_TOKEN: InjectionToken<any> = new InjectionToken<any>('CHECKOUT_IZIPAY_TOKEN');
