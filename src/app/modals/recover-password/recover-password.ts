import { InjectionToken } from "@angular/core";
import { ComponentType } from "ngx-toastr";

export const RECOVER_PASSWORD_TOKEN: InjectionToken<any> = new InjectionToken<any>('RECOVER_PASSWORD_TOKEN');
